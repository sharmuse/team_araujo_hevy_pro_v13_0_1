# Server config
PORT=4000
JWT_SECRET=supersecret-jwt-key-change-me

# SMTP (configure to enable real emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=username
SMTP_PASS=password
SMTP_FROM="Team Araújo Hevy Pro <no-reply@teamaraujo.app>"
